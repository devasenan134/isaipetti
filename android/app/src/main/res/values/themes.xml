<resources>
    <style name="Theme.Isaipetti" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
    <!-- See-through, so the lock screen shows faintly behind the lyrics. -->
    <style name="Theme.Isaipetti.LockLyrics" parent="Theme.Isaipetti">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <color name="launch_background">#FF16131F</color>
    <color name="launcher_background">#FF1F1A2E</color>
</resources>
